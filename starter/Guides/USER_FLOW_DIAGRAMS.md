# 🔄 User Flow Diagrams

## 📱 User Journey Map

```
┌──────────────────────────────────────────────────────────────────────┐
│                         NEW USER SIGN UP                              │
└──────────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
                    ┌──────────────────────────┐
                    │   /onboarding Page       │
                    │  (UserRoleSelection)     │
                    └──────────────────────────┘
                                   │
                    ┌──────────────┼──────────────┐
                    │              │              │
                    ▼              ▼              ▼
        ┌─────────────────┐  ┌──────────┐  ┌─────────────┐
        │ Looking for     │  │ List     │  │ Become      │
        │ Property?       │  │ Property │  │ Agent       │
        └─────────────────┘  └──────────┘  └─────────────┘
                │                  │              │
                ▼                  ▼              ▼
          /search            /landlord      /agent/signup
                            /new-property
```

---

## 🎯 Agent Sign-Up Flow

```
┌──────────────────────────────────────────────────────────────────────┐
│                    AGENT REGISTRATION FLOW                            │
└──────────────────────────────────────────────────────────────────────┘

Step 1: Basic Info
┌────────────────────────────────────┐
│ • Full Name                         │
│ • Email (auto-filled)              │
│ • Phone Number                     │
│ • Business Name                    │
│ • Years of Experience              │
│ • Specializations (multi-select)   │
│ • License Number                   │
│ • Service Areas                    │
│ • About Me                         │
│ • Deals Closed                     │
└────────────────────────────────────┘
              │
              ▼
Step 2: Document Verification
┌────────────────────────────────────┐
│ • Upload Agent License             │
│   (PDF/JPG/PNG, max 5MB)          │
│ • Upload Business Registration     │
│   (PDF/JPG/PNG, max 5MB)          │
│ • Agree to Terms & Conditions      │
└────────────────────────────────────┘
              │
              ▼
Step 3: Review & Submit
┌────────────────────────────────────┐
│ Review all entered information     │
│ Documents: 2/2 uploaded            │
└────────────────────────────────────┘
              │
              ▼
        ┌───────────┐
        │  Submit   │
        └───────────┘
              │
              ▼
    Database Updated:
    • user_type = 'agent'
    • agent_verification_status = 'pending'
    • agent_specialization = [...]
    • agent_business_name = "..."
              │
              ▼
    ┌──────────────────────────┐
    │  Success Message         │
    │  "Application Submitted" │
    │  Review: 24-48 hours     │
    └──────────────────────────┘
```

---

## 💰 Service Request Flow (Client Side)

```
┌──────────────────────────────────────────────────────────────────────┐
│                   PREMIUM SERVICE REQUEST FLOW                        │
└──────────────────────────────────────────────────────────────────────┘

         Client visits /service-request
                    │
                    ▼
         ┌──────────────────────┐
         │ Premium Check        │
         │ isPremium?           │
         └──────────────────────┘
                │         │
         NO ────┘         └──── YES
         │                      │
         ▼                      ▼
┌──────────────────┐   ┌────────────────────┐
│ PayPal Payment   │   │ Select Agent       │
│ $49.99 USD       │   │ (from verified     │
│ (~JMD 7,745)     │   │  agents list)      │
└──────────────────┘   └────────────────────┘
         │                      │
         │ Success              │
         ▼                      ▼
    Activate Premium      ┌─────────────────────┐
    (30 days)             │ Fill Requirements   │
         │                │ • Property Type     │
         │                │ • Location          │
         └───────────────▶│ • Budget Range      │
                          │ • Bedrooms/Bathrooms│
                          │ • Timeline          │
                          │ • Requirements      │
                          └─────────────────────┘
                                    │
                                    ▼
                          ┌─────────────────────┐
                          │ Submit to API       │
                          │ POST /api/service-  │
                          │ requests/premium    │
                          └─────────────────────┘
                                    │
                ┌───────────────────┴───────────────────┐
                ▼                                       ▼
    ┌───────────────────────┐              ┌───────────────────────┐
    │ Create service_request│              │ Create notification   │
    │ in database           │              │ in agent_notifications│
    └───────────────────────┘              └───────────────────────┘
                │                                       │
                └───────────────────┬───────────────────┘
                                    ▼
                          ┌─────────────────────┐
                          │ Success Toast       │
                          │ "Request sent!"     │
                          └─────────────────────┘
```

---

## 🔔 Agent Notification Flow

```
┌──────────────────────────────────────────────────────────────────────┐
│                     AGENT NOTIFICATION SYSTEM                         │
└──────────────────────────────────────────────────────────────────────┘

Agent logs in → /agent/notifications
            │
            ▼
   ┌─────────────────────────┐
   │ GET /api/agents/        │
   │ notifications/:agentId  │
   └─────────────────────────┘
            │
            ▼
   ┌─────────────────────────┐
   │ Fetch notifications     │
   │ with service_requests   │
   │ and user details        │
   └─────────────────────────┘
            │
            ▼
   ┌─────────────────────────────────────────┐
   │ Display in Dashboard:                    │
   │                                          │
   │ ┌─────────────────────────────┐         │
   │ │ 🔔 Service Requests    [3 New]│        │
   │ └─────────────────────────────┘         │
   │                                          │
   │ Filter: [All] [Unread]                  │
   │                                          │
   │ ┌─────────────────────────────┐         │
   │ │ 🆕 John Doe                 │         │
   │ │ Residential • Kingston       │         │
   │ │ JMD 5M - 15M                │         │
   │ │ [Click to expand]           │         │
   │ └─────────────────────────────┘         │
   │                                          │
   │ Expanded View:                           │
   │ ┌─────────────────────────────┐         │
   │ │ Contact: john@example.com   │         │
   │ │ Phone: +1876-555-1234       │         │
   │ │ Property: 3 bed, 2 bath     │         │
   │ │ Timeline: Urgent (ASAP)     │         │
   │ │                             │         │
   │ │ [Contact Client]            │         │
   │ │ [Mark as Read]              │         │
   │ └─────────────────────────────┘         │
   └─────────────────────────────────────────┘
            │
            ▼
   Auto-refresh every 30 seconds
```

---

## 🗄️ Database Relationships

```
┌─────────────────────────────────────────────────────────────────┐
│                      DATABASE SCHEMA                             │
└─────────────────────────────────────────────────────────────────┘

┌──────────────────┐
│     users        │
│──────────────────│
│ id (PK)          │◄────┐
│ email            │     │
│ full_name        │     │
│ user_type        │     │
│ agent_verified   │     │
│ ...agent fields  │     │
└──────────────────┘     │
         │               │
         │               │
         ▼               │
┌──────────────────┐     │
│service_requests  │     │
│──────────────────│     │
│ id (PK)          │     │
│ user_id (FK)     │─────┘ (Client)
│ agent_id (FK)    │─────┐ (Agent)
│ property_type    │     │
│ location         │     │
│ budget_min/max   │     │
│ bedrooms         │     │
│ bathrooms        │     │
│ timeline         │     │
│ requirements     │     │
│ status           │     │
└──────────────────┘     │
         │               │
         │               │
         ▼               │
┌──────────────────┐     │
│agent_notifications│    │
│──────────────────│     │
│ id (PK)          │     │
│ agent_id (FK)    │◄────┘
│ service_request_id│◄────┘
│ notification_type │
│ is_read          │
│ read_at          │
└──────────────────┘

Relationships:
• service_requests.user_id → users.id (client)
• service_requests.agent_id → users.id (agent)
• agent_notifications.agent_id → users.id
• agent_notifications.service_request_id → service_requests.id
```

---

## 📊 State Flow Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                    REQUEST STATUS FLOW                           │
└─────────────────────────────────────────────────────────────────┘

Client submits request
         │
         ▼
   ┌──────────┐
   │ PENDING  │ ← Initial status
   └──────────┘
         │
         │ Agent views notification
         ▼
   ┌──────────┐
   │  VIEWED  │ ← Agent opened request
   └──────────┘
         │
         │ Agent contacts client
         ▼
   ┌──────────┐
   │ MATCHED  │ ← Property matches found
   └──────────┘
         │
         │ Client satisfied
         ▼
   ┌──────────┐
   │COMPLETED │ ← Request fulfilled
   └──────────┘

Alternative path:
   ┌──────────┐
   │ REJECTED │ ← Agent declines or no match
   └──────────┘
```

---

## 🔐 Authentication Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                    AUTHENTICATION FLOW                           │
└─────────────────────────────────────────────────────────────────┘

User visits protected page
         │
         ▼
   ┌──────────────┐
   │ Clerk Auth   │
   │ Check        │
   └──────────────┘
         │
    ┌────┴────┐
    │         │
Signed In   Signed Out
    │         │
    ▼         ▼
┌─────────┐ ┌──────────────┐
│ Show    │ │ Redirect to  │
│ Content │ │ /sign-in     │
└─────────┘ └──────────────┘
    │
    ▼
Get user.id from Clerk
    │
    ▼
Check database for user record
    │
    ├─ Not exists? → Create user
    └─ Exists? → Load profile
         │
         ▼
    User authenticated
    + Profile loaded
```

---

## 🎯 Key Integration Points

```
Frontend Components ↔ API Routes ↔ Supabase Database
                  ↔ Clerk Auth
                  ↔ PayPal SDK

1. AgentSignup
   → POST /api/agents/signup
   → Updates users table
   → Sets agent_verification_status

2. PremiumServiceRequest
   → GET /api/agents/verified
   → POST /api/service-requests/premium
   → Creates service_request + notification

3. AgentNotificationCenter
   → GET /api/agents/notifications/:agentId
   → PUT /api/agents/notifications/:agentId
   → Fetches/updates notifications

4. PayPal Integration
   → Creates order
   → Captures payment
   → POST /api/user/upgrade-premium
   → Activates premium access
```

---

## ⏱️ Auto-Refresh System

```
Agent Dashboard Component
         │
         ▼
   useEffect Hook
         │
         ├─ Initial load: fetchNotifications()
         │
         └─ setInterval(30000) ← Every 30 seconds
                  │
                  ▼
         fetchNotifications()
                  │
                  ▼
         Update state with new data
                  │
                  ▼
         Re-render dashboard
                  │
                  ▼
         Show unread badge if new items
```

---

## 🎨 Component Hierarchy

```
App (_app.js)
│
├─ /onboarding
│  └─ UserRoleSelection
│     ├─ Role Card (Buyer)
│     ├─ Role Card (Landlord)
│     ├─ Role Card (Agent)
│     └─ Role Card (Service Request)
│
├─ /agent/signup
│  └─ AgentSignup
│     ├─ Step 1: Basic Info Form
│     ├─ Step 2: Document Upload
│     └─ Step 3: Review & Submit
│
├─ /service-request
│  └─ PremiumServiceRequest
│     ├─ PayPal Payment Gate
│     ├─ Agent Selection Grid
│     └─ Requirements Form
│
└─ /agent/notifications
   └─ AgentNotificationCenter
      ├─ Filter Tabs (All/Unread)
      ├─ Notification List
      │  └─ Notification Card
      │     ├─ Quick Info
      │     └─ Expandable Details
      │        ├─ Client Contact
      │        ├─ Property Details
      │        └─ Action Buttons
      └─ Auto-refresh Logic
```

This visual guide helps understand the complete system architecture and user flows!
